(function ($, document, window, Parse) {
	"use strict";

	$(function() {
		//	Verify login with parse
		var parseLogin = function(number, name) {
			//	Returns a promise with success or failure
			var promise;
			return promise;
		};

		



	});


}(window.$, document, window, window.Parse));